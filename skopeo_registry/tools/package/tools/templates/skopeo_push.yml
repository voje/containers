127.0.0.1:5000:
    tls-verify: false
    credentials:
        username:
        password:
    images:
        it_bacula-db:
        nginx:

# Btw: yaml is not a valid destination format.
# Working push commands:
# $ skopeo sync --src=docker --dest=docker --src-tls-verify=false --dest-tls-verify=false 127.0.0.1:5000/nginx:1.15.10 k-vm-repo-server.docker.iskratel.mak:4567/deploy/portainer_templates

# Note: after removing the image from the destination registry (web dashboard), we immediately run this command and it fails.   
# After waiting a bit and rerunning the command, it succeedes.   
# Subsequent runs also work fine.   
# $ skopeo sync --src=yaml --dest=docker --dest-tls-verify=false skopeo_push.yml k-vm-repo-server.docker.iskratel.mak:4567/deploy/portainer_templates
