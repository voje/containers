FROM ubuntu

ARG DEBIAN_FRONTEND=noninteractive

RUN apt-get update -y && apt-get install -y \
    vim \
    iputils-ping \
    nmap \
    traceroute \
    iproute2 \
    netcat \
    ncat \
    mtr \
    git \
    nghttp2 \
    dnsutils \
    curl \
    gnupg2 \
    wget \
    tcpdump 

